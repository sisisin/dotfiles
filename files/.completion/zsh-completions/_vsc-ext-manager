#compdef vsc-ext-manager

_vsc-ext-manager() {
  _values 'sub_command'\
    'check-diff[check diff vscode and master file]' \
    'install[install exts from master file]' \
    'uninstall[uninstall exts which is not exists master file]' \
    'overwrite[overwrite exts which is already installed to master file]' \
    'install-and-append-to-file[install and overwrite]'
}
